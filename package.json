{
  "name": "bng-metric-digital-prototype",
  "version": "0.1.1",
  "description": "bng-metric-digital-prototype. GOV.UK Prototype Kit with CDP Platform running capabilities",
  "author": "Defra DDTS",
  "license": "(OGL-UK-3, MIT) see licenses in /LICENSES",
  "engines": {
    "node": ">=24.14.1"
  },
  "scripts": {
    "dev": "node -e \"process.env.PORT='3010'; const child = require('node:child_process').spawn('govuk-prototype-kit', ['dev'], { stdio: 'inherit', shell: true }); child.on('exit', code => process.exit(code));\"",
    "serve": "govuk-prototype-kit serve",
    "start": "govuk-prototype-kit start",
    "lib:link": "npm link ../bng-library",
    "lib:unlink": "npm uninstall --no-save bng-library && npm install",
    "lib:status": "node -e \"const fs=require('node:fs');const p='node_modules/bng-library';try{const s=fs.lstatSync(p);console.log('bng-library: '+(s.isSymbolicLink()?('LINKED -> '+fs.realpathSync(p)):'installed from pin (not linked)'))}catch{console.log('bng-library: not installed')}\"",
    "likec4": "npx likec4 start",
    "likec4:export": "docker run --rm -v \"$PWD:/data\" -w /data likec4/likec4 export png -o ./arch-diagrams",
    "format": "prettier --write \"**/*.{cjs,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{cjs,js,json,md}\"",
    "postinstall": "node scripts/postinstall.mjs",
    "setup:husky": "node -e \"try { (await import('husky')).default() } catch (e) { if (e.code !== 'ERR_MODULE_NOT_FOUND') throw e }\" --input-type module",
    "install:gitleaks": "node scripts/install-gitleaks.mjs",
    "test": "npm run test:unit && npm run test:features",
    "test:unit": "node --test \"test/**/*.test.js\"",
    "test:features": "cucumber-js features/"
  },
  "dependencies": {
    "@cucumber/cucumber": "13.2.1",
    "@defra/interactive-map": "0.0.1-alpha",
    "@govuk-prototype-kit/common-templates": "3.0.0",
    "@ministryofjustice/frontend": "9.0.0",
    "archiver": "^8.0.0",
    "better-sqlite3": "13.0.3",
    "bng-library": "github:DEFRA/bng-library#af307d543e0dd0a1e6d1fe9a90496d63beedc2c3",
    "dotenv": "^17.2.3",
    "govuk-frontend": "6.4.0",
    "govuk-prototype-kit": "13.20.3",
    "lru-cache": "11.5.2",
    "multer": "^2.2.0",
    "ol": "^10.10.0",
    "ol-mapbox-style": "^13.4.2",
    "shpjs": "^6.1.0",
    "undici": "8.10.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "prettier": "3.9.6"
  },
  "overrides": {
    "better-sqlite3": "$better-sqlite3"
  }
}
